### To install on OSX/Linux (Make sure you have latest versions of all packages)
    npm install
    pip install virtualenv
    virtualenv venv
    source venv/bin/activate
    pip install -r requirements.txt
    npm run rebuild
    npm start